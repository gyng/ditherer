## 024 Chain Inline Add Row

- Move the primary add-filter affordance into the filter chain list so it feels like part of the chain rather than a detached control.
- Render a final placeholder row inside the chain list with no numbering, drag handle, checkbox, or entry action buttons.
- Keep the existing utility toolbar actions (browse, presets, random chain, save/load/share/clear) intact while simplifying the add-filter interaction.
- Preserve the existing inline filter combobox behavior so adding a filter still uses the same searchable picker.
